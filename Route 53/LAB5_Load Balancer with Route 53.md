AWS Console > EC2 > Create ec2 linux instance > Connect to the Linux instance > Install apache webserver 
 " sudo -i
   yum install httpd -y
   service httpd start 
   cd /var/www/html
   echo "Testing" >index.html "  
> Target groups > create target group for the ec2 instance > Load balancers > create application load balancer > add the       target group to the alb > 
   
