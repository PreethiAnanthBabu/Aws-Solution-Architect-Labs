AWS Console > EC2 > Launch EC2 Linux instance > Connect to the Linux instance > Install apache webserver 
 " sudo -i
   yum install httpd -y
   service httpd start "
> Create a domain in Godaddy > AWS Console > Route 53 > Create a hosted zone with the domain created in Go daddy
> GO Daddy > add the AWS Route 53 name servers into GoDaddy > AWS Console > Route 53 > Create Records in the hosted Zone
> Give the Linux intance public IP > Create record > Now give the domain name in browser and we get the website page displayed
