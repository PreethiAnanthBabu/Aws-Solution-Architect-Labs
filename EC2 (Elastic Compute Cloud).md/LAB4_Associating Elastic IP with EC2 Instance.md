AWS Console > EC2 > Launch Instance > Give Name , keypair(.pem file), select AMI(Windows) and 
network settings (add rdp and http port from anywhere) > Launch Instance > Elastic IP > Allocate Elastic IP address 
> select the created IP > Actions > Associate IP address > select the Ec2 instance > Associate > Now we can the EC2 instance with
the Elastic IP
