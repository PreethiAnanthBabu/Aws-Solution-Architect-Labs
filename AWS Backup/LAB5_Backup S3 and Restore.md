AWS Console > S3 > Create bucket > Add objects to the bucket >AWS Backup > Vaults > Create new Vault > Dashboard > On-demand backup > Select Resource type > Choose Vault > Select IAM Role >
> Create On-demand backup Vault > Select the Created vault > We can see S3 there
> Actions > Restore > Now another S3 is created with same name but with new ARN
