AWS Console > RDS > Create Database > Select mssql > Give required details > create database > Connect to the mssql database using mssql server management studio >
> Create new query > AWS Backup 
> Vaults > Create new Vault > Dashboard > On-demand backup > Select Resource type > Choose Vault > Select IAM Role >
> Create On-demand backup Vault > Select the Created vault > We can see RDS there
> Actions > Restore > Now another RDS is created with same name but with new endpoint
