# AWS Managed Policy:
Steps:
   AWS Console > IAM > Users > Create user >  Give User name > Check access to managaement console > 
   Check IAM user > Next > Attach policy directly > Add required policy > Create user > Download .csv file > 
   Login to console with the downloaded crendentials 

# Customer Managed Policy:
Steps:
   AWS Console > IAM > Policy > Create Policy > Select a service > Add the required policy > Next > 
   Give the policy name > Create Policy > Users > Select user > Attach policy > Select the created policy > Add
